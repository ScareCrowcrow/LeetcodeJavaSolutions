package summarize;

/*
* function fn(n) {
    // 第⼀一步：判断输⼊入或者状态是否⾮非法？
    if (input/state is invalid) {
        return;
    }
    // 第⼆二步：判读递归是否应当结束?
    if (match condition) {
        return some value;
    }
    // 遍历所有可能出现的情况
    for (all possible cases) {
        // 第三步: 尝试下⼀一步的可能性
        solution.push(case)
        // 递归
        result = fn(m)
        // 第四步：回溯到上⼀一步
        solution.pop(case)
    }
}
*
* 例题见 L0039
*
* */

public class Backtracking {
}
